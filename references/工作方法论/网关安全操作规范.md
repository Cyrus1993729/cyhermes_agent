# 网关安全操作规范

## 核心原则
任何需要停 gateway 的维护，把 停→操作→重启 串成一条 && 命令，失败自动 || 回滚。

## 严禁
分步执行（停了网关用户就联系不上，中间任何一步失败即成死局）

## 标准模板
```bash
hermes gateway stop && mv old new && hermes gateway start || (mv new old && hermes gateway start)
```
