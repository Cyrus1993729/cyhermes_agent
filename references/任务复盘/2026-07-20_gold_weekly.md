# 任务复盘：黄金周报
- 日期：2026-07-20
- 任务目标：生成本周（07-13 ~ 07-19）黄金积存金周报
- 数据质量：
  - SGE: fx_fallback=true（自动换算补充，正常）
  - FRED: TIPS/利差数据完整
  - 金价API校验: COMEX vs XAU现货偏差 0.08%（正常）
  - ⚠️ USDCNH汇率: main.py内部7.25 vs API 6.78，偏差+6.9%（关键发现）
- 实际流程（偏离预期的点）：
  - main.py 首次路径检测在 MSYS bash 中失败（Windows路径问题），改用 /c/Users/... 格式解决
  - USDCNH 汇率严重滞后，导致公允价值高估、境内需求评分失真。此问题在数据校验阶段被捕获并在报告中详细标注
  - L1 审查经历4轮：R1(2 FAIL) → R2(4 FAIL) → R3(1 FAIL) → R4(CONDITIONAL)
  - 第1-3轮FAIL集中在：精确点位预测（违反契约边界）、符号/术语混乱（折价-6.5%语义重复）、汇率偏差基准不明确
- 踩过的坑：
  1. 最初版本在4.2/4.3中给了精确价位（$3,900-$4,200等），违反契约"不预测精确价位点位"边界 → 改为定性描述
  2. "折价-6.5%"表述语义重复（折价本身已含"低于"含义，再加负号矛盾） → 统一为"实际价相对公允价值低6.5%"，并定义术语约定
  3. 数据校验表中~0.003%偏差来源不明 → 改为"<0.01%"
  4. main.py dxy信号解释缺少前值对比（"走弱"需证明）→ 补充"近1月走弱0.8%"
  5. 模拟场景下事实引用（FOMC日期、Fed Chair Warsh、WGC数据）无法验证 → 添加[模拟]标签和数据场景说明
- 最终结果：CONDITIONAL（L1 4轮，零FAIL，4 CONDITIONAL均为模拟场景事实源验证）
- 可优化点：
  - main.py 的 USDCNH 数据源需修复（接入实时汇率API）
  - 评分映射公式（50 + 加权得分 × 25.5）应在报告和脚本中显式文档化
  - 契约中可增加"模拟数据场景"的标注要求，避免L1反复因事实验证触发CONDITIONAL

## 🔄 教训回流判断

| 发现 | 类型 | 是否写入lessons.md |
|:---|:---|:---|
| USDCNH汇率数据滞后影响全部分析 | 数据管道问题 | 否（一次性，需修复脚本而非规则） |
| "折价-6.5%"语义重复 | 术语规范 | 否（契约已要求口径一致，本次是执行问题） |
| 模拟场景需显式标注[模拟] | 方法论 | 是 → L12 |
| dxy信号需补充前值对比 | 数据逻辑 | 否（属于审查发现的具体问题） |
