# Q7 交付物 — Memory 引擎化

## 1. 每周 Memory 体检 Cron Job

```
job_id: 39bef06ff753
名称: 每周 Memory 体检
调度: 0 10 * * 1 (每周一上午10:00)
下次运行: 2026-07-06T10:00:00+08:00
动作: 运行 scripts/mem_gc.py → 人话总结 → 发微信
工具: terminal, file
状态: enabled, scheduled
```

## 2. 矛盾检测记忆规则

已写入 MEMORY.md（memory 目标，15条/76%/1689字）：

> 矛盾检测规则：当 Agent 在任务中遇到与 memory 里某条记录明显矛盾的事实（比如 memory 说某服务已关闭但实际在运行，或记录的工具版本与当前不符），必须主动告知用户"我发现一条记忆跟现实不符：[矛盾内容]。要更新吗？"——不要默默忽略，也不要自作主张直接改。

## 3. 用户确认

用户在 Q7 对话中确认："第一样不需要，第二样可以推动一样"——确认① cron 已创建（用户原话"第1条和第2条可以做一下，第三条就不用了"），② 矛盾检测规则已写入。
