---
name: post-task-review
description: "每次任务收尾时强制触发复盘。当用户表示任务完成、开始聊下一个话题、或你准备结束对话时，先检查是否满足复盘条件，满足就生成。| 被以下重任务 skill 的工作流配方引用：xiaohongshu-analysis、serenity-tweet-analysis、gold-investment-analysis、bilibili-understand、supply-chain-ripple-analysis。"
triggers:
  - 用户说"好了""可以了""OK 就这样""Noted""收到"
  - 任务连续性中断（切换到新话题）
  - 你即将结束一轮对话且未生成复盘
---

# 任务复盘强制触发

## 触发条件（满足任一即触发）

1. 工具调用 >= 5 次
2. 用户明确说"复盘"
3. 任务持续超过 10 分钟
4. 涉及环境修改（安装、配置变更、文件修改）

## ⚠️ 阶段完成 checkpoint（新增）

**多步骤操作完成后，必须先汇报再问下一步。** 不要闷头连跑十几步然后跳过总结直接问"要不要继续 Q2"。

触发点：一个分析问题/优化阶段结束时（如 Q1 三步做完 → Q2 开始前），主动给出：
- 做了什么（几步、改了什么、结果）  
- 当前状态（删了几个 skill、加了几个依赖）  
- 下一步是什么

反例（2026-07-01）：完成 Q1 的三步技能优化（合并/改描述/加依赖，15+ 工具调用）后，直接跳到"继续 Q2？"没有总结汇报。用户反馈："既然做完了，你为什么不主动告诉我呢？"

### ⚠️ 交付物呈报前 checkpoint（2026-07-02 新增）

**任何方案/分析/提案在展示给用户之前，必须先检查是否需要 L1 审查。**

触发点：你即将向用户展示一个完成的工作成果（方案、分析报告、配置建议、draft 文档等）。

执行：
1. 暂停——这份内容是不是"非琐碎交付物"？（≥3 条实质性内容的方案 = 是）
2. 如果是 → **先跑 `scripts/qwen_review.py`，审查通过再展示**
3. 审查 FAIL/CONDITIONAL → 修复后再展示，不要直接给用户看未审查的方案

反例（2026-07-02）：Agent 完成了备份方案 draft，直接展示给用户。用户问"你的这个方案qwen审过吗"——没有。浪费一轮交互，且失信于用户。正确做法：写完方案 → 创建 contract → 跑 qwen_review → 修好 → 展示。

## 执行流程

1. 当感知到任务收尾（用户说 OK/好了/Noted/下一个话题），**立即检查当前 session 的工具调用次数和是否涉及环境修改**
2. 如果满足任一条件 → 按模板生成复盘 → 存到 `references/任务复盘/YYYY-MM-DD_任务名.md`
3. 如果期间已生成过 → 跳过（不重复）
4. 复盘完成后，在回复中简短提一句"复盘已存"，不啰嗦

## 模板

```
# 任务复盘：{任务名}
- 日期：
- 任务目标：
- 实际流程（偏离预期的点）：
- 踩过的坑：
- 最终结果：
- 可优化点：
```

## 🔄 教训回流（Q5 自改进闭环）

复盘写完后，必须多问一步：**"这次暴露的是一次性问题，还是可复用的规则？"**

**可复用的 → 写入 `hermes/lessons.md`**：
- 格式：ID(L+序号) + 来源(日期+任务) + 规则 + 证据(复现次数)
- 按任务类型归类（内容分析/投资分析/系统操作/通用方法论）
- 如果同一条规则已在 lessons.md 中存在 → **复现次数 +1**，不新增
- 复现次数 = lessons.md 里的累计命中次数（非 review_log）

**一次性的 → 不加**：
- 只在这一次复盘里记录，不进 lessons.md

**写入后检查**：
- 这条规则和 lessons.md 里已有的规则冲突吗？
- 有没有同类旧规则可以被它替代？（替代 → retire 旧的）
- 这条规则有没有引用证据？（没有证据不能加）

## 反例（本次出错的原因）

- ❌ 规则存在 Memory 中，被 20+ 条记忆稀释
- ❌ 没有收尾 checkpoint 触发检查
- ✅ 现在改为 Skill，每次任务收尾自动加载检查

## 🔗 审查管线 + 自改进闭环（已落地 2026-07-02）

- `scripts/qwen_review.py` — L1 千问审查，直连 API
- `skills/productivity/l1-review/SKILL.md` — L1 审查调用指南
- `skills/productivity/sprint-contract/SKILL.md` — 任务启动契约
- `hermes/lessons.md` — 经验教训库，复盘可复用规则写回此处
- `scripts/review_trend.py` — 纵向评估趋势
- `hermes/safety_invariants.md` — 安全不变量
- `hermes/model_routing.md` — 模型路由规则（L1→Opus 升级路径）

原始设计方案见 [`references/review-pipeline-design.md`](references/review-pipeline-design.md)。
